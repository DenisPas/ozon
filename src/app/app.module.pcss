.app {
	max-width: 960px;
	margin: 0 auto;
	padding: 24px;
}

.nav {
	display: flex;
	gap: 16px;
	padding-bottom: 16px;
	border-bottom: 1px solid rgb(128 128 128 / 30%);
}

.active {
	font-weight: 700;
	text-decoration: underline;
}

.main {
	padding-top: 24px;
}
